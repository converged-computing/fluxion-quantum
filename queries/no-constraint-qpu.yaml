# EXPECT: SATISFIED. No constraint -> nothing pruned -> qpu found anywhere.
# Control case for "unspecified constraint matches everything."
version: 9999
resources:
  - type: slot
    count: 1
    label: device
    with:
      - type: qpu
        count: 1
attributes:
tasks:
  - command: ["sampler"]
    slot: device
    count:
      per_slot: 1